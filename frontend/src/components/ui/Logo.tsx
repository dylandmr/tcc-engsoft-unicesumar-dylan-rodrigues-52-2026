/** The Prompt Arena wordmark: an ignition square + display type. */
export function Logo() {
  return (
    <span className="inline-flex items-center gap-3 font-display font-bold tracking-wide text-bright">
      <span className="size-3.5 rounded-sm bg-ignition" />
      <span className="text-lg">PROMPT ARENA</span>
    </span>
  )
}
